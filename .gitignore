# Local env and secrets
.env
.env.local
.env.development
.env.production
.env.*.local
*.pem
*.key

# Dependencies
node_modules/

# Frontend build / cache
.next/
out/
dist/
*.tsbuildinfo

# Go build / test artifacts
*.exe
*.test
coverage.out
coverage/

# OS / editor
.DS_Store
Thumbs.db
.idea/

# Local scanner / SBOM output (CI uploads artifacts; do not commit)
security-reports/
sbom/
govulncheck.jsonl
govulncheck-db.jsonl

# Local PostgreSQL backup / restore drill artifacts (sensitive; never commit)
var/backups/
